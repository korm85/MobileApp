# PocketMind

Native Android chatbot application with configurable model endpoint, model name, system prompt, temperature, reasoning toggle, modern chat UI, keyboard-safe composer, and artifact preview.

The installable debug APK is built automatically by GitHub Actions on every push to `main`. Open **Actions → Build PocketMind APK**, select the latest successful run, and download the **PocketMind-APK** artifact.
